import './index.scss';
import Sidebar from '../Sidebar/index';

const Layout = () => {
    return (
        <div className='App'>
            <Sidebar />
        </div>
    )
};

export default Layout;